package constants

// RemoteBuildDir is the remote build dir.
const RemoteBuildDir = "/builds"

// DefaultConfigFileName is the default name of config file.
const DefaultConfigFileName = ".docker-tools.yaml"
